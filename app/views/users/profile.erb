<h1>User Profile</h1>

You're logged in as user.id 
<%= current_user.id %><br>

Name: <%= @user.name %><br>
Email: <%= @user.email %><br>

<a href="/users/<%= @user.id %>/edit">
<Button>Edit Profile</Button>
</a>

<a href="/users/logout">
<Button>Logout</Button>
</a>

Properties Listed:<br><br>
<% @user.properties.each_with_index do |property, index| %>
	<%= index + 1 %>) <a href="/properties/<%= property.id %>"><%= property.title %>, Location: <%= property.location %>, Price: RM<%= property.price %>/month</a>
	<br>
	<% end %>
